version: 4.0.78
steps:
  - prompt: Focus on the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: >-
      Assert that the logo is visible on the homepage.  # Use the content of the
      file from the environment
    commands:
      - command: assert
        expect: the logo is visible on the homepage
