version: 4.0.78
steps:
  - prompt: Click on "Education" in the navigation menu.
    commands:
      - command: hover-text
        text: Education
        description: link in the navigation menu
        action: click
  - prompt: Assert that the education section page loads.
    commands:
      - command: assert
        expect: Assessment Made Easy is visible on the page
  - prompt: Assert that the education section page loads.
    commands:
      - command: assert
        expect: Education Resources are visible on the page
